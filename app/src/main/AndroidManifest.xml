<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todor.blueteam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity
            android:name=".MainMenuActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenuChoices.StartingLineup" />
        <activity android:name=".MainMenuChoices.Subtitutes" />
        <activity android:name=".MainMenuChoices.Reserves" />
        <activity android:name=".MainMenuChoices.ClubOwner" />
        <activity android:name=".MainMenuChoices.Coach"></activity>
    </application>

</manifest>